FROM mcr.microsoft.com/devcontainers/base:bookworm

# Use the pre-created non-root user that devcontainers/base provides
USER vscode

# Keep the container alive regardless of how it’s launched
CMD ["bash", "-lc", "sleep infinity"]